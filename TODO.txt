[x] add common endpoints
[x] make custom endpoints 

[x] clean up code and scripts
[x] add custom HTTP codes by name instead of number in errors.js
[ ] user scoring of events should use event _id 
[ ] script automate replacing the old id with _id
[ ] script remove user custom id 
[ ] test code backend is working with testdbdev

[x] write documentation in backend API.md in docs 
[x] make a requirements docs for each section 
  [x] e.g. backend node npm 
  [x] full app docker docker-compose
  [x] nginx proxy nginx 

[x] add https to nginx reverse proxy with certs in ./nginx/security being put inside the container and used

[x] add scripts to export databases to ./databases/data (keep a raw copy in ./database/raw)

[ ] write report explaining application parts (backend + database + docker cluster + nginx)
[ ] write presentation (store in ./docs)

[ ] branch backend with final deliverable
[ ] zip deliverable (only backend) and submit
